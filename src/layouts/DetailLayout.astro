---
import Popover from "./Popover.astro";

export interface Props {
  day: number;
}

const { day } = Astro.props as Props;
---

<Popover transition={`day-${day}`}>
  <slot name="header" />
  <slot />
</Popover>
