---
import Coordiante from "./Coordinate.astro";

export interface Props {
  cx: string;
  cy: string;
  [key: string]: string;
}

const { cx, cy, ...props } = Astro.props as Props;
---

<g>
  <circle cx={cx} cy={cy} {...props}></circle>
  <g class="coordinates">
    <Coordiante x={cx} y={cy} />
  </g>
</g>
