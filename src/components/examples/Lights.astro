---
import Lightbulb from "./Lightbulb.astro";
import Lamp from "./Lamp.astro";

export interface Props {
  title?: string;
  size: number;
}

const { title, size } = Astro.props as Props;
---

<Lightbulb title={title} size={size} />
<Lamp title={title} size={size} />
