---
day: 5
title: How to Draw Basic Paths with SVG
component: Arrow
category: The Path Element
description: In this example we draw a hamburger menu icon and simple arrow and learn how to draw basic paths with SVG.
suggestions: [quadratic-bezier, cubic-bezier, arc, svg-in-css]
---

import SVG from "../../components/markdown/elements/SVG.astro";
import Path from "../../components/markdown/elements/Path.astro";
import Example from "../../components/markdown/layouts/Example.astro";
import Fullscreen from "../../components/markdown/examples/Fullscreen.astro";
import Final from "../../components/markdown/layouts/Final.astro";
import "./basic-path.scss";

Once we covered [basic shapes](/svg/basic-shapes), it's time to move on to the `element:path` element. The path is the most powerful SVG element. We can define pretty much anything with paths and if you open any SVG file, you will mostly see paths.

The shape of a path is defined by the `property:d` attribute. Here we define several drawing commands. A command always starts with a letter defining the command type and ends with a coordinate.

Here we only have the two most basic commands, move-to and line-to. The move-to command moves the cursor to a point without drawing a line and the line-to command draws a straight line from the previous point. A command always continues the previous command. When we draw a line we only define the endpoint. The starting point will be the previous command’s endpoint.

### Hamburger menu icon

Before we get to the arrow example, let's draw a simple hamburger menu icon. We draw three lines within the same path with the move-to (`value:M`) and line-to (`value:L`) commands. First, we move to the start of the top line with `value:M -45, -45` and draw a line to its end with `value:L 45, -45`. Hover over the coordinates in the code or on the image to see how they are positioned.

<Example>

<SVG width="200" height="200" viewBox="-100 -100 200 200">
  <Path
    d="M -45, -45 L 45, -45"
    stroke="#333333"
    stroke-width="15"
    stroke-linecap="round"
    data-element-id="path-1"
  />
</SVG>

{/* prettier-ignore */}
```html {7-9}
<svg
  width="200"
  height="200"
  viewBox="-100 -100 200 200"
>
  <path
    d="
        M -45, -45
        L  45, -45"
    stroke="#333333"
    stroke-width="15"
    stroke-linecap="round"
  />
</svg>
```

</Example>

Then we continue and draw to more lines the same way. What is cool about paths, is that they don't have to be continuous. We can have several move-to commands within the same path. The commas between the X and Y coordinates are optional. We skip them this time.

<Example>

<SVG width="200" height="200" viewBox="-100 -100 200 200">
  <Path
    d="M -45 -45 L 45 -45 M -45 0 L 45 0 M -45 45 L 45 45"
    stroke="#333333"
    stroke-width="15"
    stroke-linecap="round"
    data-element-id="path-1"
  />
</SVG>

{/* prettier-ignore */}
```html ins={10-13}
<svg
  width="200"
  height="200"
  viewBox="-100 -100 200 200"
>
  <path
    d="
        M -45 -45
        L  45 -45 
        M -45   0 
        L  45   0 
        M -45  45 
        L  45  45"
    stroke="#333333"
    stroke-width="15"
    stroke-linecap="round"
  />
</svg>
```

</Example>

Icons like this often feel like noise on HTML. In a later chapter, we learn how to inline this [icon in CSS](/svg/svg-in-css) and use it as you see it at the top left corner of this page.

### Heart shape icon

Here's another example made with a move to command and two line to commands.

<Example>

<SVG width="200" height="200" viewBox="-100 -100 200 200">
  <Path
    d="M -30 -20 L 0 10 L 30 -20"
    fill="none"
    stroke="red"
    stroke-width="80"
    stroke-linecap="round"
    data-element-id="path-1"
  />
</SVG>

{/* prettier-ignore */}
```html
<svg
  width="200"
  height="200"
  viewBox="-100 -100 200 200"
>
  <path
    d="
        M -30 -20 
        L   0  10 
        L  30 -20"
    fill="none"
    stroke="red"
    stroke-width="80"
    stroke-linecap="round"
  />
</svg>
```

</Example>

In the example above if we reduce the value of the `property:stroke-width` property, then we realize that the code above is actually a simple V shape.

<Example>

<SVG width="200" height="200" viewBox="-100 -100 200 200">
  <Path
    d="M -30 -20 L 0 10 L 30 -20"
    fill="none"
    stroke="red"
    stroke-width="10"
    stroke-linecap="round"
    data-element-id="path-1"
  />
</SVG>

{/* prettier-ignore */}
```html 'stroke-width="10"'
<svg
  width="200"
  height="200"
  viewBox="-100 -100 200 200"
>
  <path
    d="
        M -30 -20 
        L   0  10 
        L  30 -20"
    fill="none"
    stroke="red"
    stroke-width="10"
    stroke-linecap="round"
  />
</svg>
```

</Example>

### Exercise: How would you do a full-screen icon?

Did you know you can put not only videos on full screen but any other web elements as well? Click the icon below to see how it works.

How would you do the full-screen icon below? Check out this [YouTube tutorial](https://youtu.be/jX3mIQdQQ2w?si=oisc74y6ouJp_JZf) to learn how.

Hint: You can always right-click on an SVG in the browser, select Inspect, and check its source code.

<Fullscreen />

### Arrow icon

Then to get to our example for today, we can draw an arrow in a very similar way. We start with a line in the middle, then we continue the line to draw the upper wing.

<Example>

<SVG width="200" height="200" viewBox="-100 -100 200 200">
  <Path
    d="M -70 0 L 70 0 L 30 -50"
    fill="none"
    stroke="#D1495B"
    stroke-width="25"
    stroke-linecap="round"
    stroke-linejoin="round"
    data-element-id="path-1"
  />
</SVG>

{/* prettier-ignore */}
```html 'stroke-linejoin="round"'
<svg
  width="200"
  height="200"
  viewBox="-100 -100 200 200"
>
  <path
    d="
        M -70 0 
        L 70 0 
        L 30 -50"
    fill="none"
    stroke="#D1495B"
    stroke-width="25"
    stroke-linecap="round"
    stroke-linejoin="round"
  />
</svg>
```

</Example>

You might note that we also had a `property:stroke-linejoin` property in the previous example. This works similarly to `property:stroke-linecap`, but this one affects the connections of the path segments. If we remove this property, the path will look as follows:

<Example>

<SVG width="200" height="200" viewBox="-100 -100 200 200">
  <Path
    d="M -70 0 L 70 0 L 30 -50"
    fill="none"
    stroke="#D1495B"
    stroke-width="25"
    stroke-linecap="round"
    data-element-id="path-1"
  />
</SVG>

{/* prettier-ignore */}
```html del={15}
<svg
  width="200"
  height="200"
  viewBox="-100 -100 200 200"
>
  <path
    d="
        M -70 0 
        L 70 0 
        L 30 -50"
    fill="none"
    stroke="#D1495B"
    stroke-width="25"
    stroke-linecap="round"
    stroke-linejoin="round"
  />
</svg>
```

</Example>

Then we can finish the line with moving to the end of the horizontal line again, and drawing a straight line downwards to draw the lower wing of the arrow. You might notice that at the bottom of this and other pages in the navigation button we include a similar SVG.

<Final>

```html ins="M 70 0 L 30 50"
<svg width="200" height="200" viewBox="-100 -100 200 200">
  <path
    d="M -70 0 L 70 0 L 30 -50 M 70 0 L 30 50"
    fill="none"
    stroke="#D1495B"
    stroke-width="25"
    stroke-linecap="round"
    stroke-linejoin="round"
  />
</svg>
```

</Final>
